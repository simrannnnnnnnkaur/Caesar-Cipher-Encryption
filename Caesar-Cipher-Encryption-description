#What is Ceasar-Cipher_Encryption:

It’s one of the simplest encryption techniques.
Each letter in your text is shifted by a certain number of positions in the alphabet.

Example:

With a shift of 3:
A → D, B → E, C → F, … and X → A, Y → B, Z → C.

So, "HELLO" becomes "KHOOR" when shifted by 3.

